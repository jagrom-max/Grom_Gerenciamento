# App

Esta pasta foi reservada para o codigo Laravel do novo GROM.

Estrutura desejada:
- `Domain/`
- `Application/`
- `Infrastructure/`
- `Http/`
- `Support/`

Proxima etapa tecnica:
- instalar `php` e `composer`;
- gerar o scaffold Laravel dentro desta area;
- migrar primeiro o dominio `Access`, seguido pelo piloto `Produtividade`.

